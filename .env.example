# Local fallbacks for the Kubernetes Downward API values.
# In a cluster these are injected by chart/templates/deployment.yaml.
POD_NAME=local-pod
POD_IP=127.0.0.1
NODE_NAME=local-node

# Gunicorn / Flask runtime
PORT=8000
LOG_LEVEL=info
